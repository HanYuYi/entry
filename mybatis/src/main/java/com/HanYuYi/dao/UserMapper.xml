<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.HanYuYi.dao.UserMapper">
    
    <resultMap id="userResultMap" type="user">
        <result column="userName" property="names"/>
    </resultMap>

    <!--user resultType使用别名-->
    <select id="getUserInfo" resultType="user">
        SELECT * FROM user_base;
    </select>

    <!--使用resultMap映射-->
    <select id="getUserById" parameterType="long" resultMap="userResultMap">
        SELECT * FROM user_base WHERE id = #{id};
    </select>

    <insert id="insertUser" parameterType="user">
        INSERT INTO user_base (id, userCode, userName, userPassword, gender, birthday, phone, address, userRole, createBy, createDate, modifyBy, modifyDate)
        VALUES
        (#{id}, #{userCode}, #{userName}, #{userPassword}, #{gender}, #{birthday}, #{phone}, #{address}, #{userRole}, #{createBy}, #{createDate}, #{modifyBy}, #{modifyDate});
    </insert>

    <delete id="deleteUserById" parameterType="long">
        DELETE FROM user_base WHERE id = #{id};
    </delete>

    <update id="updateUser" parameterType="map">
        UPDATE user_base SET phone=#{phone}, birthday=#{birthday} WHERE id=#{id};
    </update>
</mapper>